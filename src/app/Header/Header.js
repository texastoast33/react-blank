const Header = React.createClass({
    render: function() {
        return (
            <header className="header">
                This is a header. Add login button
            </header>
        )
    }
});